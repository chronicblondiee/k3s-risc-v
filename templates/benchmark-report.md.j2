# Node benchmark: {{ inventory_hostname }} - {{ bench_timestamp }}

Generated by `playbooks/11_riscv64_node_benchmark.yml`. Raw tool output is
kept verbatim (not parsed excerpts) so reports stay directly comparable
across k3s builds / kernel updates. Sections are omitted when their
benchmark was skipped (e.g. a `--tags cpu` run).

## Context

- Kernel: `{{ ctx_kernel.stdout | default('n/a') }}`
- OS: {{ ansible_distribution }} {{ ansible_distribution_version }}
- RAM: {{ ansible_memtotal_mb }} MB
- Disk: `{{ ctx_disk.stdout | default('n/a') }}`
- k3s: `{{ ctx_k3s.stdout | default('n/a (k3s not responding)') }}`
- fio: `{{ ctx_fio_ver.stdout | default('n/a') }}`
- sysbench: `{{ ctx_sysbench_ver.stdout | default('n/a') }}`
- iperf3: `{{ ctx_iperf_ver.stdout | default('n/a') }}`
- Pods running during benchmark (background load): {{ ctx_pod_count.stdout | default('n/a') }}

### lscpu

```
{{ ctx_lscpu.stdout | default('n/a') }}
```
{% if sysbench_cpu_single.stdout is defined or sysbench_cpu_multi.stdout is defined %}

## CPU (sysbench)
{% if sysbench_cpu_single.stdout is defined %}

### Single-thread ({{ sysbench_cpu_single.cmd | join(' ') }})

```
{{ sysbench_cpu_single.stdout }}
```
{% endif %}
{% if sysbench_cpu_multi.stdout is defined %}

### All cores ({{ sysbench_cpu_multi.cmd | join(' ') }})

```
{{ sysbench_cpu_multi.stdout }}
```
{% endif %}
{% endif %}
{% if sysbench_mem_write.stdout is defined or sysbench_mem_read.stdout is defined %}

## Memory (sysbench)
{% if sysbench_mem_write.stdout is defined %}

### Write ({{ sysbench_mem_write.cmd | join(' ') }})

```
{{ sysbench_mem_write.stdout }}
```
{% endif %}
{% if sysbench_mem_read.stdout is defined %}

### Read ({{ sysbench_mem_read.cmd | join(' ') }})

```
{{ sysbench_mem_read.stdout }}
```
{% endif %}
{% endif %}
{% if fio_seq_write.stdout is defined %}

## Storage - host fio (direct I/O against {{ bench_tmp_dir }} on the NVMe root fs)

### Sequential write (1M blocks, iodepth 8)

```
{{ fio_seq_write.stdout }}
```

### Sequential read (1M blocks, iodepth 8)

```
{{ fio_seq_read.stdout | default('n/a') }}
```

### Random write (4k blocks, iodepth 32)

```
{{ fio_rand_write.stdout | default('n/a') }}
```

### Random read (4k blocks, iodepth 32)

```
{{ fio_rand_read.stdout | default('n/a') }}
```
{% endif %}
{% if iperf_loopback.stdout is defined or iperf_lan_rx.stdout is defined %}

## Network (iperf3)
{% if iperf_loopback.stdout is defined %}

### Node loopback (TCP-stack baseline, stays on-box)

```
{{ iperf_loopback.stdout }}
```
{% endif %}
{% if iperf_lan_rx.stdout is defined %}

### LAN receive (control machine -> node)

```
{{ iperf_lan_rx.stdout }}
```
{% endif %}
{% if iperf_lan_tx.stdout is defined %}

### LAN transmit (node -> control machine, iperf3 -R)

```
{{ iperf_lan_tx.stdout }}
```
{% endif %}
{% endif %}
{% if fio_pvc_seq_write.stdout is defined %}

## Storage - in-cluster fio (local-path PVC, same NVMe via hostPath bind mount)

### Sequential write (1M blocks, iodepth 8)

```
{{ fio_pvc_seq_write.stdout }}
```

### Random read (4k blocks, iodepth 32)

```
{{ fio_pvc_rand_read.stdout | default('n/a') }}
```
{% endif %}
{% if iperf_pod.stdout is defined %}

## Network - in-cluster iperf3 (host -> pod IP through the CNI)

Same-box like the loopback test above, so the delta vs loopback is the
CNI/veth path cost, not the NIC.

```
{{ iperf_pod.stdout }}
```
{% endif %}
